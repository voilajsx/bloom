/**
 * Bloom Framework - Clean Application Entry Point
 * @module @voilajsx/bloom
 * @file src/app.tsx
 */

import { initializeBloom } from '@/platform/bloom'

// Initialize and export Bloom app
const App = initializeBloom()

export default App